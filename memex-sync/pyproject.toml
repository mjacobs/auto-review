[project]
name = "memex-sync"
version = "0.1.0"
description = "Periodic D1 -> Postgres sync of serverless-memex captures into the memex schema."
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "mj" }]
dependencies = [
  "click>=8.1",
  "httpx>=0.27",
  "psycopg[binary]>=3.2",
  "pydantic>=2.7",
  "pydantic-settings>=2.4",
]

[project.scripts]
memex-sync = "memex_sync.cli:main"

[dependency-groups]
dev = [
  "pytest>=8.3",
  "pytest-cov>=5.0",
  "respx>=0.21",
  "ruff>=0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/memex_sync"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
