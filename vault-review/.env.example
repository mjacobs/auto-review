# Path to the Obsidian vault root. Defaults to ~/vault (resolved in config.py
# as Path.home()/"vault"); uncomment and set an absolute path to override.
# Note: values here are NOT shell-expanded, so write a literal path, not $HOME.
# VAULT_PATH=/path/to/vault

# Timezone for day boundaries.
TZ=America/Los_Angeles
